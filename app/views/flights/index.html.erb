

<h3>Current Flights:</h3>
<div>
<ol>
<% @flights.each do |flight| %>
  <% if current_user.admin %>
  <li><strong><%= link_to flight.flight_number, flight_path(current_user, flight) %></strong> | <%= link_to 'Edit', edit_flight_path(current_user, flight) %> |  <%= link_to 'Delete',flight_path(current_user, flight), method: :delete %></li>
  <% else %>
  <li><%= link_to flight.flight_number, flight_path(current_user, flight) %></li> 
  <% end %>
<% end %>
</ol>
</div>
<%= link_to 'Back', user_path(current_user) %>